name("environment.pl").
main_file("environment.pl").

% Optional: Custom package search paths (recommended for local dev dependencies)
% scryer_path([
%     "../other-package/scryer-manifest.pl"
% ]).

% Dependencies are standard Scryer Prolog libraries (no external packages needed)
